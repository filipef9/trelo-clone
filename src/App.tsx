import { Column } from './Column';
import { AppContainer, ColumnContainer } from './styles';

export const App = () => {
  return (
    <AppContainer>
      <Column text="Todo:" />
    </AppContainer>
  )
}

